# Mikke
友達特定SNS

25 冬墓イブ


## Configuration
### installation
```bash
cd ./client npm ci



```

### githooks

`./githooks/example.config.env` を `./githooks/config.env` にコピーして，中身を適当に書き換える．

```bash
git config core.hooksPath .githooks
chmod -R +x ./
```
